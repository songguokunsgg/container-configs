version: '3.8'
services:
  RK3568:
    image: openwrt
    container_name: OpenWRT
    # 请根据需求调整
    # volumes:
    #   - "/user/openwrt:/home/openwrt/openwrt"
    network: bridge
    command: bash
    tty: true
    stdin_open: true